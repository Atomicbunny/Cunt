var Bot = require('discord-bot');
 
var bot = new Bot({
    email: <email>,
    password: <pass>
});
 
bot
    .on(bot.triggers.command, 'Say')
    .do(function(bot, conf, args) {
        this.reply('Ni**er');
    });
 
bot.connect();
