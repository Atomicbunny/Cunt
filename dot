var Bot = require('discord-bot');
 
var bot = new Bot({
    email: <ultracunt.gmail.com>,
    password: <password>
});
 
bot
    .on(bot.triggers.command, 'Hello there')
    .do(function(bot, conf, args) {
        this.reply('faggots');
    });
 
bot.connect();
